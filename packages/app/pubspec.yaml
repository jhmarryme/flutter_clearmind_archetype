name: flutter_clearmind_archetype_app
description: "app"
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_clearmind_archetype_shared:
    path: ../shared
  flutter_clearmind_archetype_resource:
    path: ../resource
  flutter_clearmind_archetype_domain:
    path: ../domain
  flutter_bloc: 8.0.1
  flutter_screenutil: ^5.9.0
  get_it: 7.6.0
  injectable: 2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: 2.4.5
  flutter_lints: 2.0.1
  freezed: 2.3.5
  freezed_annotation: 2.2.0
  injectable_generator: 2.1.6
flutter:

